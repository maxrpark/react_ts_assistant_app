import React from 'react';
import { useAppContext } from '../context';
type Props = {};

const Home = (props: Props) => {
  // const  = useAppContext();
  return <div>Home</div>;
};

export default Home;
